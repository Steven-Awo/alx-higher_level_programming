#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    list_of_keys = list(a_dictionary.keys())
    for value_dicry in list_of_keys:
        if val == a_dictionary.get(value_dicry):
            del a_dictionary[value_dicry]
    return (a_dictionary)
